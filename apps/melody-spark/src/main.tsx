import React from 'react';
import ReactDOM from 'react-dom/client';
import { MelodySparkApp } from './components/MelodySparkApp';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MelodySparkApp />
  </React.StrictMode>,
);
